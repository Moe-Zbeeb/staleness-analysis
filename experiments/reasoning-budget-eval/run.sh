set -euo pipefail
ROLLOUT_ROOT=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
export PYTHONPATH="$ROLLOUT_ROOT${PYTHONPATH:+:$PYTHONPATH}"
export CUDA_DEVICE_ORDER=PCI_BUS_ID
export HF_HUB_OFFLINE=1 HF_DATASETS_OFFLINE=1 OMP_NUM_THREADS=4 TOKENIZERS_PARALLELISM=false VLLM_WORKER_MULTIPROC_METHOD=spawn PYTHONUNBUFFERED=1
"${GENERATION_PYTHON:?Set GENERATION_PYTHON to the vLLM environment Python}" "$ROLLOUT_ROOT/run.py" --model-index "${SLURM_ARRAY_TASK_ID:?Submit as an array with indices 0-4}"
