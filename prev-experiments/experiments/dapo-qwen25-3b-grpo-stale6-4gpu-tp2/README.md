# Qwen2.5-3B staleness ≤6 retry

Fresh retry of failed job 2141854. Two trainer GPUs and inference TP2/DP1 with one API server. Normal priority, four A100 GPUs on deep-chungus-9. The 25-step smoke gate must pass before the 1,000-step production run. Existing failed evidence and all active jobs are preserved. Scientific settings match the prior cap6 attempt; inference topology and run paths differ.
